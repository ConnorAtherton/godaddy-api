require 'spec_helper'

describe GodaddyApi do
  it 'has a version number' do
    expect(GodaddyApi::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
